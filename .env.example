# Auth.js — random secret used to sign/encrypt JWTs. Generate with: npx auth secret
AUTH_SECRET=

# Auth.js — canonical site URL. Not needed for local dev (inferred from the
# request); set this in production, especially behind a reverse proxy.
AUTH_URL=https://nsisongeffiong.com

# Comma-separated list of email addresses allowed to sign in to /admin
ADMIN_EMAILS=you@example.com

# bcrypt hash of the admin password — generate with: npx tsx scripts/hash-password.ts <password>
# Next.js expands $-prefixed tokens in .env values, so escape every literal $
# in the hash as \$ (e.g. \$2b\$10\$...) or it will be silently mangled.
ADMIN_PASSWORD_HASH=

# Direct PostgreSQL connection string for Drizzle migrations
# Settings → Database → Connection string → URI (use the "direct" connection, not pooled)
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# Pooled connection string for runtime queries (use this in production)
DATABASE_URL_POOLED=postgresql://postgres.your-project:your-password@aws-0-region.pooler.supabase.com:6543/postgres

# Your site URL — used for RSS feed and Disqus embed URLs
NEXT_PUBLIC_SITE_URL=https://nsisongeffiong.com

# Your Disqus shortname — find this in disqus.com/admin → Settings → General
NEXT_PUBLIC_DISQUS_SHORTNAME=nsisongeffiong
