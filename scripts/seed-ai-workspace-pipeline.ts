/**
 * Seed script — inserts the ai-workspace-scripts pipeline post
 *
 * Usage:
 *   npx tsx --env-file=.env scripts/seed-ai-workspace-pipeline.ts
 */

import { db } from '../lib/db'
import { posts } from '../lib/db/schema'

const content = "<p>The frustration is familiar. You ask an AI to write code, get output that's mostly right, then spend time catching the gaps: edge cases it didn't consider, security issues it glossed over, documentation it skipped. You're doing the review work yourself every time.</p>\n\n<p>The question worth asking: what if the review was part of the pipeline?</p>\n\n<p>That's the idea behind <code>ai-workspace-scripts</code>. A pipeline that takes a task description and passes it through four stages automatically \u2014 one model writes the code, a second reviews it, a third audits it, the first synthesises the feedback and ships the final version. Each stage commits to a Git feature branch. You review the output and merge when satisfied.</p>\n\n<p>The repo is at <a href=\"https://github.com/nsisongeffiong/ai-workspace-scripts\">github.com/nsisongeffiong/ai-workspace-scripts</a>.</p>\n\n<hr />\n\n<h2>The pipeline model</h2>\n\n<p>Four stages. Three models. One feature branch per run.</p>\n\n<table>\n<tr><th>Stage</th><th>Model</th><th>Role</th></tr>\n<tr><td>1</td><td>Claude Opus 4.6</td><td>Initial implementation</td></tr>\n<tr><td>2</td><td>GPT-5.4</td><td>Code quality & documentation review</td></tr>\n<tr><td>3</td><td>Gemini 2.5 Flash</td><td>Security & correctness audit</td></tr>\n<tr><td>4</td><td>Claude Opus 4.6</td><td>Final synthesis</td></tr>\n</table>\n\n<p>The choice to use three different models rather than one was deliberate. Each model has different blind spots. GPT catches things Claude misses. Gemini's security lens is distinct from GPT's quality lens. Claude at Stage 4 has the context of both reviews and the authority to act on them.</p>\n\n<p>Using the same model at every stage means the same blind spots at every review. The diversity is the point.</p>\n\n<hr />\n\n<h2>Why commit per stage</h2>\n\n<p>Every stage commits its output to the feature branch before the next stage runs.</p>\n\n<pre><code class=\"language-python\">if from_stage &lt;= 1:\n    stage_1_claude_code(task)\n    git_commit(repo, \"feat(claude): initial implementation\", [PROJECT_ROOT / \"src\"])\n\nif from_stage &lt;= 2:\n    stage_2_gpt_review()\n\nif from_stage &lt;= 3:\n    stage_3_gemini_validate()</code></pre>\n\n<p>The <code>--from-stage</code> flag is what makes this useful. If a provider returns a 503 mid-run, you don't re-run everything before it. You resume:</p>\n\n<pre><code class=\"language-bash\">python scripts/run.py --from-stage 3 \"your task\"</code></pre>\n\n<p>Stage 1 and 2 outputs are already in the commit history. Stage 3 picks up exactly where things stopped.</p>\n\n<p>The alternative is a pipeline that either succeeds completely or forces a full restart. At the cost of a few extra git commits, the resumable model is strictly better.</p>\n\n<hr />\n\n<h2>How `orchestrate.py` works</h2>\n\n<p>The core logic lives in a single file. Here's the shape of it:</p>\n\n<pre><code class=\"language-python\">def run(task: str, from_stage: int = 1) -&gt; None:\n    repo = Repo(PROJECT_ROOT)\n\n    if from_stage &gt; 1:\n        branch = repo.active_branch.name\n    else:\n        branch = f\"feature/{int(time.time())}\"\n        repo.git.checkout(\"-b\", branch)\n\n    if from_stage &lt;= 1:\n        stage_1_claude_code(task)\n        git_commit(repo, \"feat(claude): initial implementation\", ...)\n\n    if from_stage &lt;= 2:\n        stage_2_gpt_review()\n\n    if from_stage &lt;= 3:\n        stage_3_gemini_validate()\n\n    if from_stage &lt;= 4:\n        stage_4_claude_final()\n        git_commit(repo, \"review(claude): final synthesis\", ...)</code></pre>\n\n<p>New run: creates a <code>feature/{timestamp}</code> branch and starts from Stage 1. Resume: stays on the current branch and starts from whatever stage you specify. Each run gets its own branch, so every pipeline execution is independently traceable in git history.</p>\n\n<hr />\n\n<h2>The file extraction problem</h2>\n\n<p>Stage 1 asks the model to write code. The model returns text. The pipeline has to turn that text into actual files on disk.</p>\n\n<p>The convention: annotate every code block with a filepath on the fence line.</p>\n\n<pre><code></code></pre>\n\n<p>// component code here</p>\n\n<pre><code></code></pre>\n\n<p>The extractor parses this and writes each block to the correct path:</p>\n\n<pre><code class=\"language-python\">pattern1 = re.compile(\n    rf\"```(?:{LANGS})\\s+([\\w./\\-]+\\.(?:{EXT}))\\n(.*?)```\",\n    re.DOTALL\n)\nfor m in pattern1.finditer(text):\n    write_file(PROJECT_ROOT / m.group(1), m.group(2))</code></pre>\n\n<p>If no filepath is found, the fallback writes everything to <code>src/generated.py</code>. You don't want that often. When it happens, it means the task prompt wasn't explicit enough about the convention. The fix is one line in your project's <code>CONTEXT.md</code>:</p>\n\n<pre><code>Filepath on every opening fence line: ```tsx src/components/Hero.tsx</code></pre>\n\n<p>After that, files go to the right place.</p>\n\n<hr />\n\n<h2>The context injection problem</h2>\n\n<p>The pipeline is project-agnostic by design. But projects have specific requirements \u2014 design tokens, coding conventions, framework rules \u2014 that need to reach Stage 1 without bloating every stage's token budget.</p>\n\n<p>The solution: context is opt-in per prompt via a <code>{brand_context}</code> placeholder.</p>\n\n<pre><code class=\"language-python\">def load_prompt(name: str) -&gt; str:\n    text = p.read_text(encoding=\"utf-8\")\n    if \"{brand_context}\" in text:\n        tokens_path = PROJECT_ROOT / \"prompts\" / \"BRAND_TOKENS.md\"\n        brand_path  = PROJECT_ROOT / \"prompts\" / \"BRAND.md\"\n        if tokens_path.exists():\n            brand = tokens_path.read_text(encoding=\"utf-8\")\n        elif brand_path.exists():\n            brand = brand_path.read_text(encoding=\"utf-8\")\n        else:\n            brand = \"\"\n        text = text.replace(\"{brand_context}\", brand)\n    return text</code></pre>\n\n<p>Only prompts that include <code>{brand_context}</code> get the context injected. The security audit prompt doesn't need design tokens, so it doesn't get them.</p>\n\n<p>For projects with large context files, there's a pre-flight distillation step: Claude compresses your full <code>BRAND.md</code> into a <code>BRAND_TOKENS.md</code>, under 100 lines, containing only what Stage 1 actually needs. A full brand guide might run to 4000 tokens. The distilled version runs around 800. That's 3200 tokens back in the budget for code.</p>\n\n<hr />\n\n<h2>What the workspace structure looks like</h2>\n\n<pre><code>~/ai-workspace/\n  .shared/\n    .env              \u2190 API keys (chmod 600)\n    prompts/          \u2190 default stage prompts\n    orchestrate.py    \u2190 the pipeline runner\n    .venv/            \u2190 shared Python environment\n\n  projects/\n    your-project/\n      prompts/\n        claude_coder.md    \u2190 Stage 1 system prompt (project override)\n        BRAND.md           \u2190 project context (optional)\n        BRAND_TOKENS.md    \u2190 distilled tokens (auto-generated)\n      reviews/\n        review-gpt.md      \u2190 Stage 2 output\n        review-gemini.md   \u2190 Stage 3 output\n        final-review.md    \u2190 Stage 4 output\n      scripts/\n        run.py             \u2190 project entry point\n      src/                 \u2190 generated source code</code></pre>\n\n<p>Each project overrides the shared prompts by placing its own versions in <code>prompts/</code>. The pipeline checks the project's <code>prompts/</code> first, then falls back to the shared ones. The workspace itself stays untouched: one workspace, many projects.</p>\n\n<hr />\n\n<h2>What the source-reading stages do</h2>\n\n<p>Stages 2, 3, and 4 need to read what Stage 1 wrote. Reading everything in <code>src/</code> is the naive approach. On any real project that's too much context \u2014 config files, lock files, generated types, things the reviewing model doesn't need to see.</p>\n\n<p>The smarter approach: diff against the initial scaffold commit and read only the files the pipeline actually changed. Config files, lock files, generated types \u2014 none of that reaches the reviewing model.</p>\n\n<pre><code class=\"language-python\">initial = repo.git.rev_list(\"--max-parents=0\", \"HEAD\").strip().splitlines()[-1]\nchanged = set(repo.git.diff(\"--name-only\", initial, \"HEAD\").strip().splitlines())\npipeline_files = sorted(\n    PROJECT_ROOT / f for f in changed\n    if (PROJECT_ROOT / f).is_file()\n)</code></pre>\n\n<p>The reviewing stages see only what changed. The context stays focused and the reviews stay accurate.</p>\n\n<hr />\n\n<h2>Getting started</h2>\n\n<p>Install the workspace with one command:</p>\n\n<pre><code class=\"language-bash\">curl -fsSL https://raw.githubusercontent.com/nsisongeffiong/ai-workspace-scripts/main/setup-workspace.sh -o setup-workspace.sh\nbash setup-workspace.sh</code></pre>\n\n<p>The setup script asks for your three API keys (Anthropic, OpenAI, Google), stores them in <code>~/.ai-workspace/.shared/.env</code> with <code>chmod 600</code>, installs all dependencies, and runs a smoke test against all three APIs.</p>\n\n<p>Then create a project and run a task:</p>\n\n<pre><code class=\"language-bash\">bash ~/new-project.sh my-project\ncd ~/ai-workspace/projects/my-project\npython scripts/run.py \"Build a landing page with a hero section and three feature cards\"</code></pre>\n\n<p>The pipeline creates a <code>feature/{timestamp}</code> branch, runs all four stages, commits at each step, and prints the branch name when done. Open a PR from that branch to main when the output looks right.</p>\n\n<hr />\n\n<h2>What I'd change</h2>\n\n<p>Two things, with hindsight.</p>\n\n<p><strong>Git tags per stage, not just commits.</strong> Resuming from Stage 3 requires knowing the branch is still active and the right commits are in history. A tag like <code>stage-1-complete</code> would make this explicit: checkout the tag directly rather than infer from commit history.</p>\n\n<p><strong>Typed task schemas.</strong> A task is currently a free-text string. A structured schema with explicit fields for language, framework, files to create, and constraints would make Stage 1 more predictable and the filepath convention less dependent on the prompt author remembering to include it.</p>\n\n<p>Both are solvable. The pipeline works well as-is for the problems it was built to solve.</p>\n\n<hr />\n\n<p>The repo is at <a href=\"https://github.com/nsisongeffiong/ai-workspace-scripts\">github.com/nsisongeffiong/ai-workspace-scripts</a>. The setup takes about ten minutes. If you run into anything, open an issue.</p>"

const post = {
  type: 'tech' as const,
  title: 'Building a Multi-Model AI Development Pipeline',
  slug: 'building-multi-model-ai-pipeline',
  excerpt: 'A four-stage pipeline that chains Claude, GPT, and Gemini to write, review, and ship code automatically. Git commits at every stage mean a failed API call never requires starting over.',
  tags: ['AI/ML', 'devtools', 'python', 'bash', 'automation'],
  published: true,
  publishedAt: new Date('2026-04-22'),
  metadata: {
    readTime: 14,
    featured: true,
    codeLanguages: ['python', 'bash'],
    legacyDisqus: false,
  },
  content,
}

async function seed() {
  console.log(`Seeding post: "${post.title}"`)

  try {
    await db.insert(posts).values(post)
    console.log(`  ✓ Inserted: "${post.title}"`)
  } catch (error: any) {
    if (error?.code === '23505') {
      console.log(`  ↷ Already exists: "${post.title}" — skipping`)
    } else {
      console.error(`  ✗ Failed: "${post.title}"`, error.message)
    }
  }

  process.exit(0)
}

seed()
